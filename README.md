## Installation instructions:
The module is installed in the AB namespace. It should be created in Magento 2 in the app/code/AB directory. 
Go to the AB directory and use the git clone command to copy the module. 
The AB_CustomPromoApi directory will be created. The directory should be renamed to CustomPromoApi.
